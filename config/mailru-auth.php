<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | OAuth credentials
    |--------------------------------------------------------------------------
    |
    | Кабинет приложений: https://o2.mail.ru/app/
    | Документация:       https://id.vk.com/about/business/go/docs/ru/vkid/latest/oauth/oauth-mail/index
    |
    */

    'client_id' => env('MAILRU_CLIENT_ID'),

    'client_secret' => env('MAILRU_CLIENT_SECRET'),

    /*
    |--------------------------------------------------------------------------
    | Redirect URI
    |--------------------------------------------------------------------------
    |
    | По умолчанию: APP_URL/mailru/callback.
    | Должен совпадать с тем, что прописан в настройках OAuth-приложения.
    |
    */

    'redirect_uri' => env(
        'MAILRU_REDIRECT_URI',
        rtrim((string) env('APP_URL', 'http://localhost'), '/') . '/mailru/callback'
    ),

    /*
    |--------------------------------------------------------------------------
    | Модель пользователя
    |--------------------------------------------------------------------------
    |
    | null — модель берётся автоматически из MoonShine guard
    |        (config/auth.providers.<moonshine-guard-provider>.model).
    |        Это гарантирует, что найденный по email пользователь и guard,
    |        в который потом происходит loginUsingId(), работают с одной
    |        и той же таблицей. Подходит в 99% случаев.
    |
    | Явный класс нужен, только если MoonShine guard использует не Eloquent-
    | провайдер (например, JWT/Sanctum/API-токены) или если требуется искать
    | пользователя в отдельной таблице, отличной от модели guard'а.
    |
    | Пример: 'user_model' => \App\Models\MoonshineUser::class,
    |
    */

    'user_model' => null,

    /*
    |--------------------------------------------------------------------------
    | Поле для поиска пользователя
    |--------------------------------------------------------------------------
    */

    'email_field' => 'email',

    /*
    |--------------------------------------------------------------------------
    | Автоматическая регистрация роутов
    |--------------------------------------------------------------------------
    |
    | Если false — нужно вручную подключить routes/web.php из пакета или
    | определить свои роуты с именами mailru.auth и mailru.callback.
    |
    */

    'register_routes' => true,

];
